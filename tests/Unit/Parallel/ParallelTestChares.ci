// Distributed under the MIT License.
// See LICENSE.txt for details.

module ParallelTestChares {
  template <typename Metavariables>
  chare[migratable] TestChare {
    entry TestChare(Parallel::CProxy_ConstGlobalCache<Metavariables>);
    entry void set_id(int);
  };

  template <typename Metavariables>
  group[migratable] TestGroupChare {
    entry TestGroupChare(Parallel::CProxy_ConstGlobalCache<Metavariables>);
  };

  template <typename Metavariables>
  nodegroup[migratable] TestNodeGroupChare {
    entry TestNodeGroupChare(Parallel::CProxy_ConstGlobalCache<Metavariables>);
  };

  template <typename Metavariables>
  array[1D] TestArrayChare {
    entry TestArrayChare(Parallel::CProxy_ConstGlobalCache<Metavariables>);
  };

  template <typename Metavariables>
  array[1D] TestBoundArrayChare {
    entry TestBoundArrayChare(Parallel::CProxy_ConstGlobalCache<Metavariables>);
  };
};
